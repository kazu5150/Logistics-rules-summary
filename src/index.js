import React from 'react';
import ReactDOM from 'react-dom';
import LogisticsRulesSummary from './LogisticsRulesSummary';

ReactDOM.render(
  <React.StrictMode>
    <LogisticsRulesSummary />
  </React.StrictMode>,
  document.getElementById('root')
);